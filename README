对论文《Continuous User Identification via Touch and Movement Behavioral Biometrics》(Sensors, 2016) 的实验复现。


# 问题:

在触屏设备上,不同的持握方式(左/右手 × 拇指/食指)会产生形态各异的滑动轨迹。通过采集用户的滑动轨迹并提取特征,可以推断出用户当时的持握模式,进而为持续身份认证提供依据。


# 数据:

在自研 iOS 采集应用中记录用户的滑动手势,每条记录包含轨迹点序列 (t, x, y)、手势模式标签(4 类)、时间戳等。方向标签(上/下/左/右)由首末点位移推断。


# 范围:

- 轨迹存储与可视化
- 每条轨迹提取 16 维特征(长度、速度、位移、形状),按手势模式与方向做分布可视化
- 训练分类器识别手势模式(暂缓)
- 分类算法评估(暂缓)

# 输出:

归一化坐标下的特征分布图,用于验证不同手势模式在各特征上的可分性,为下一步分类模型选型提供依据。